<div class="page-wrap">
  <div class="eyebrow-row"><span class="h-rule"></span><span class="h-eyebrow">Maison</span><span class="h-rule"></span></div>
  <h1>{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</div>
